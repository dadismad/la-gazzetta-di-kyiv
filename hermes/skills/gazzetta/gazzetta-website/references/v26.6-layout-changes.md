# v26.6 Layout Changes (June 12, 2026)

## Masthead
- `font-size: 1.8em` on `.masthead-name` (was 3em — too large, forced nav to wrap)
- `flex-wrap: nowrap` on `.masthead` (was wrap — nav dropped to second line, looked like separate bar)
- Nav links color: `#8B0000` dark red (was `var(--ink)` near-black)
- Nav links font-size: 13px (was 11px, below WCAG minimum)
- Nav links inside `masthead-right` div within `<header class="masthead">` (not separate `<nav>`)
- Caduceus: gold `var(--gold)`, 28×40px
- Bulavas: gold `var(--gold)`, crossed at ±32°

## Removed Elements
- `<nav class="master-nav">` — deleted from HTML entirely
- `<aside class="col-alpha">` — deleted (left sidebar: sample signals, sentiment, tickers)
- `<aside class="col-context">` — deleted (right sidebar: navigate links, gazzetta links)
- Master nav dropdown JS script — deleted
- `.nav-dropdown` CSS — kept but unused (harmless dead code)

## Single-Column Layout
- Only `col-intel` remains in `<main class="hints-lobby">`
- CSS: `.col-alpha { display: none; }`, `.col-context { display: none; }`
- Media queries also keep sidebars hidden

## Stories Always Visible
- `#storiesTeaser` class changed from `container collapsible` to `container`
- Stories body visible by default (not collapsed)
- 20 story items loaded on page load

## CSS Cleanup
- Deleted 8 old hashed CSS files from GCS (styles.2a465551.css, styles.435bac57.css, styles.4ba8a2f9.css, styles.5a6582bd.css, styles.96c672e4.css, styles.ab9a2c25.css, styles.d60c0958.css, styles.dcdc5480.css)
- Fixed orphaned `display: -ms-grid;` without selector
- Fixed media query overrides that resurrected sidebars

## Deploy Chain (MANDATORY)
1. Edit `styles.css` → hash → copy to `styles.HASH.css` → update ALL 20 HTML files → deploy all HTML + both CSS
2. Set `Cache-Control: no-cache, max-age=0` on all objects
3. Verify ALL pages load new CSS before deleting old hashes
4. See `gazzetta-verify-deploy` skill for full verification procedure
